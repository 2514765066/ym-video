import { join } from "path";

export const db = join(__dirname, "../../resources/db.json");

export const config = join(__dirname, "../../resources/config.json");

export const videoCss = join(__dirname, "../../resources/video.css");

export const videoJs = join(__dirname, "../../resources/video.js");
